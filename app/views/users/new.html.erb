<%= form_for(@user) do |f| %>

　　<%= f.label :name %>
　　<%= f.file_field :name %>

　　<%= f.label :description %>
　　<%= f.file_field :description %>

　　<%= f.label :image %>
　　<%= image_tag @user.image.thumb.url if @user.image? %><!--repair -->
　　<%= f.file_field :image %> （画像用に追加したカラムの名前）
    <%= f.hidden_field :image_cache %><!--repair -->
<% end %>