<header>
  <style>
    .thumbnail{
            margin: 0px auto;
            max-width: 200px;
        }
    table thead tr td,
    table tbody tr td {
        text-align:center;
    }
    body {
      color:#9966ff;
    }
    table tr td {
      margin-top:10px;
      margin-right:10px;
    }
    .name{
      padding-right : 20px;
    }
  </style>
  
</header>

<div class="container">
  <div class="row">
      <h1>ブログのリスト</h1>
      <table>
        <thead>
          <tr>
            <td><strong>タイトル</strong></td>
            <td><strong>名前</strong></td>
          </tr>
        </thead>
        <tbody>
          <% @blogs.each do |blog| %>
            <tr>
              <td><%= blog.title %></td>
              
          
            
              <td class="name"><%= blog.user.name %></td>
              
              <td class="name"><p class="thumbnail img-rounded img-responsive"><%=  image(blog.user) %></p></td>
              
              <td class="btn btn-success"><%= link_to 'ショー',blog %></td>
              </td>
              <% if current_user.id==blog.user.id %>
                <td class="btn btn-warning"><%= link_to '編集', edit_blog_path(blog) %></td>
                
                  <td class="btn btn-danger"><%= link_to '削除', blog, method: :delete, data: { confirm: '本当に？' } %></td>
                
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    <br/>
    
    <%= link_to '新しいブログ', new_blog_path, class:"btn btn-info" %>
    </div>
</div>
